# Four distinct vendor families for x = 4 ensemble screening.
# Copy to .env and fill in values. .env is gitignored — never commit real keys.

ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GOOGLE_API_KEY=

# Open-model provider (pick one and keep it consistent with config.json),
# e.g. Together AI, Groq, Fireworks, or OpenRouter.
OPEN_MODEL_API_KEY=
